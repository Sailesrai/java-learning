package MethodOverriding;

public class MethodOverridingDocs {

	/*
	 * --Method Overriding--
	 * # use to achieve runtime polymorphism.
	 * # There should be IS-A relationship between two classes.
	 * # Use when Parent and child do same work but methods are different.
	 * # method name and return type and list of arguments same as parent class
	 *  but implementation is different in child class is known as method overriding.
	 * 
	 * # we can't override final methods. 
	 *   final class ,methods can't be inherited
	 *   
	 * # 
	 */
}
