package javaCollection;

public class JavaColectioDocs {
    /*
     * -java collection-----
     * 
     * use to store and processing dynamic multiple data/object in java
     * 
     *  1> List : dynamic array
     *      a> ArrayList : processing slow, better to use for data storage
     *      b> LinkedList: processing fast, better to use for data processing
     *  
     *  2> Set : collection of unique data.
     *     a> HashSet : unique data only
     *     b> TreeSet : unique + sorted data only
     *     c> LinkedHashSet: unique + insertion in order
     *  
     *  
     *  3> Map : data manage in key value,pair.
     *    a> HashMap: <k,v> pair
     *    b> TreeMap: <k,v> + sorted by key
     *    c> LinkedHashMap < k,v> + insertion in order
     * 
     * 
     */
}
